/**
 * Tier T2 -- the aliasing matrix (registered placeholder).
 *
 * A1 fills this with the complete out/a/b overlap matrix from section 3 of the
 * roadmap, including the shifted-view rows that expose A-07. Registered now so
 * the tier order and gate wiring are fixed; A0 adds no cases.
 */
export function run() {
    // intentionally empty -- filled by A1.
}
