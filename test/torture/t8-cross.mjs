/**
 * Tier T8 -- cross-package conformance (registered placeholder).
 *
 * The A-01 detector, the A-03/B-03 poison-quarantine chain, and layout
 * conformance against @zakkster/lite-bvh live here. Filled once the bvh line
 * (B0+) is standing; A0 registers the tier only.
 */
export function run() {
    // intentionally empty -- filled alongside the lite-bvh sessions.
}
