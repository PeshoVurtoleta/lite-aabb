/**
 * Tier T5 -- differential fuzz against an oracle (registered placeholder).
 *
 * A later session fills this with a brute-force ground-truth oracle and a mixed
 * op stream, comparing sorted results and replaying failures from the seed.
 * Registered now to fix the tier order; A0 adds no cases.
 */
export function run() {
    // intentionally empty -- filled by a later session.
}
